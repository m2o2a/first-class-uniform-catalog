# حاسبة السعر الفورية — ربطها بـ n8n

الموقع دلوقتي فيه صفحة "احسب السعر" (`/quote`) وزرار في كل صفحة منتج، العميل بيدخل العدد ونوع الخامة وعدد اللوجوهات ومكانها، وبيطلعله سعر تقريبي فورًا مع اقتراح لأنسب خامة.

**الوضع دلوقتي:** الحاسبة شغالة فعلاً بأرقام تجريبية (placeholder) مكتوبة في كود الموقع نفسه، عشان الميزة تشتغل من أول لحظة. الخطوات اللي تحت هتخليك تنقل الحساب لـ n8n بتاعك وتتحكم في الأرقام من هناك من غير ما تحتاج كود مرة تانية.

## 1) استورد الـ Workflow الجاهز
عندك ملف اسمه **`n8n-quote-workflow.json`** جوه نفس الزيب ده.

في n8n بتاعك: **Workflows → Import from File** واختار الملف ده. هيظهرلك 3 خطوات جاهزة: Webhook → احسب السعر → Respond to Webhook.

## 2) عدّل الأرقام على مزاجك
افتح خطوة **"احسب السعر"** (Code node) — هتلاقي في أول الكود جدول واضح فيه:
- أسعار كل خامة (قطن، توين، جبردين، قطن مخلوط)
- سعر اللوجو الواحد + فرق السعر حسب مكانه (صدر/ضهر/كم)
- خصومات الكمية (كل ما العدد يزيد، الخصم يزيد)
- جمل التوصية اللي بتظهر للعميل (زي "توين اختيار قوي ومتين")

غيّر أي رقم أو جملة هنا براحتك، من غير ما تحتاج تكلمني.

## 3) فعّل الـ Webhook واحصل على الرابط
اضغط **Activate** على الـ workflow (لازم يكون شغال دايمًا عشان الموقع يقدر يوصله).

من خطوة الـ Webhook انسخ الـ **Production URL** (مش الـ Test URL) — شكله كده:
```
https://your-n8n-domain.com/webhook/fc-quote
```

## 4) ضيف الرابط في إعدادات الموقع على Vercel
1. ادخل مشروع الموقع على vercel.com
2. **Settings → Environment Variables**
3. ضيف متغير جديد:
   - Name: `N8N_QUOTE_WEBHOOK_URL`
   - Value: الرابط اللي نسخته فوق
4. احفظ واعمل **Redeploy**.

بعد كده، أي عميل يحسب سعر من الموقع، الحساب هيتم فعليًا في n8n بتاعك بالأرقام اللي انت حطيتها، مش الأرقام التجريبية.

> لو حصل أي مشكلة في الاتصال بـ n8n (السيرفر واقف مثلاً)، الموقع مش هيقف — هيرجع تلقائي للحساب المحلي بالأرقام التجريبية لحد ما ترجع تظبط n8n.
