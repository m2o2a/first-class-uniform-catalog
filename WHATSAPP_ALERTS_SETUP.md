# استلام بيانات العميل على واتساب بتاعك مباشرة

دي خدمة مجانية اسمها **CallMeBot** بتخلي أي موقع يقدر يبعتلك رسالة واتساب على رقمك الشخصي. الإعداد لمرة واحدة بس وبعدها أي عميل يعمل تصميم ويكمل بأداة "خصّص"، هتوصلك رسالة واتساب فيها اسمه ورقمه والمنتج فورًا.

> ملاحظة: دي خدمة بسيطة مناسبة للتنبيهات الشخصية (زي إشعار بعميل جديد)، مش رقم واتساب بيزنس رسمي — بس هي أسهل وأسرع حل من غير موافقات ميتا المعقدة، ومفيش عليها أي تكلفة.

## 1) ضيف رقم البوت في الواتساب بتاعك
احفظ الرقم ده كجهة اتصال جديدة في موبايلك:

```
+34 644 59 71 67
```

## 2) ابعتله رسالة التفعيل
افتح شات مع الرقم ده في واتساب، وابعتله بالظبط الرسالة دي (من غير أي تغيير):

```
I allow callmebot to send me messages
```

هتوصلك رسالة رد فيها **API Key** بتاعك (أرقام)، احتفظ بيها.

## 3) ضيف البيانات في إعدادات الموقع على Vercel
1. ادخل مشروع الموقع على vercel.com
2. **Settings → Environment Variables**
3. ضيف متغيرين:
   - `CALLMEBOT_PHONE` = رقم موبايلك بالصيغة الدولية من غير علامة + (مثال: `201149631070`)
   - `CALLMEBOT_APIKEY` = الـ API Key اللي جالك في الرسالة
4. احفظ، وبعدين اعمل **Redeploy** للموقع عشان المتغيرات تتفعّل.

بعد كده، أي عميل يكتب اسمه ورقمه في أداة التخصيص هتوصلك رسالة واتساب فورية فيها بياناته، بالإضافة لتسجيلها في الشيت لو عملت خطوة الشيت (اختيارية ومنفصلة تمامًا — تقدر تفعّل واحدة بس أو الاتنين مع بعض).
