/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote domains here if you host product photos externally
    remotePatterns: []
  }
};

module.exports = nextConfig;
